apiVersion: v1
kind: Service
metadata:
  name: '{{ .Values.service_name }}'
  labels:
    app: '{{ .Values.service_name }}'
spec:
  type: NodePort
  selector:
    app: '{{ .Values.service_name }}'
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
